@model personapi_dotnet.Models.Entities.Persona

<h2>Detalles de la Persona</h2>

<div>
    <h4>Persona</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">CC</dt>
        <dd class="col-sm-10">@Model.Cc</dd>

        <dt class="col-sm-2">Nombre</dt>
        <dd class="col-sm-10">@Model.Nombre</dd>

        <dt class="col-sm-2">Apellido</dt>
        <dd class="col-sm-10">@Model.Apellido</dd>

        <dt class="col-sm-2">Genero</dt>
        <dd class="col-sm-10">@Model.Genero</dd>

        <dt class="col-sm-2">Edad</dt>
        <dd class="col-sm-10">@Model.Edad</dd>
    </dl>
</div>

<a href="@Url.Action("Edit", "PersonaView", new { id = Model.Cc })" class="btn btn-warning">Editar</a>
<a href="@Url.Action("Index", "PersonaView")" class="btn btn-secondary">Volver a la Lista</a>
